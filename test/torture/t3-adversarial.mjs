/**
 * T3 -- adversarial insert / play / stop sequences. Registered EMPTY for PRO0.
 *
 * The sequence-spam and mode-thrash adversarial orders belong to PRO3 (sequence
 * integrity, CP-5) once stopSequence() releases the ticker. Present so the tier
 * roster is stable and later sessions fill it in place.
 */

export async function run() {
    // intentionally empty for PRO0
}
