/**
 * T5 -- differential fuzz vs an oracle. Registered EMPTY for PRO0.
 *
 * Fuzzing the shake sum / multi-target framing against an independent oracle
 * arrives in a later session. Present so the tier roster is stable.
 */

export async function run() {
    // intentionally empty for PRO0
}
