/**
 * T8 -- cross-package poison door + base-camera parity. Registered EMPTY for PRO0.
 *
 * The base<->pro bridge parity (shared _dead semantics, base-field poison
 * quarantine) is exercised once the PRO4 base-field bridge exists. Present so the
 * tier roster is stable.
 */

export async function run() {
    // intentionally empty for PRO0
}
